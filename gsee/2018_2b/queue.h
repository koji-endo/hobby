#include <stdlib.h>
#include <stdio.h>
#include "element.h"
ELEM *root_q =NULL;
ELEM *root_q2=NULL:
int empty_q(){
	return (root_q==NULL);
}

void enqueue(char x){
	ELEM *e;
	e=(ELEM *)malloc(sizeof(ELEM));
	e->v=x;
	root_q2
	root_q2=e->p;


}
